export enum RoomTheme {
  CLASSIC = 'CLASSIC',
  NIGHT_CITY = 'NIGHT_CITY',
}

export enum TimerPhase {
  IDLE = 'IDLE',
  FOCUS = 'FOCUS',
  BREAK = 'BREAK',
  PAUSED = 'PAUSED',
}

export enum AmbientSound {
  RAIN = 'RAIN',
  LOFI_MUSIC = 'LOFIC_MUSIC',
  SEA = 'SEA',
  NONE = 'NONE',
}
